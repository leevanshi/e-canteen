from pydantic import BaseModel
from typing import Optional

class MenuBase(BaseModel):
    name: str
    price: int

class MenuCreate(MenuBase):
    pass

class MenuResponse(MenuBase):
    id: Optional[str]
    is_available: bool = True   # ✅ DEFAULT AVAILABLE

    class Config:
        orm_mode = True
